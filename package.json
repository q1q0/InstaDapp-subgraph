{
  "name": "trackusertx",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ trackusertx",
    "create-local": "graph create --node http://localhost:8020/ trackusertx",
    "remove-local": "graph remove --node http://localhost:8020/ trackusertx",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 trackusertx",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.35.0",
    "@graphprotocol/graph-ts": "^0.29.0",
    "matchstick-as": "0.5.0"
  }
}
